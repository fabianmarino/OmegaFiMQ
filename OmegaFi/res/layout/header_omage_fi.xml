<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >
    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="0.5dp"
        android:contentDescription="@string/omega_img"
        android:layout_height="60dp"
        android:src="@drawable/omega_fi_img" />
    <TextView android:id="@+id/titleMessageHeader"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="19sp"
        android:visibility="gone"
        android:text="Forgot My Password"/>
    <TextView android:id="@+id/messageForForm"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:text="Please enter your email address associate with your account"
        android:inputType="textMultiLine"
        android:textSize="16sp"
        android:visibility="gone"
        />
</LinearLayout>